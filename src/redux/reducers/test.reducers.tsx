export const initialState = {
    Code: -1,
};

export default function testReducer(state: object = initialState, action: any) {
    switch (action.type) {

        default:
            return state;
    }
}